# ------------------------------------------------------------------
# .env.example (grouped with comments) — generated 2025-09-03 05:27:32
# NOTE: Values are sample defaults. Copy to .env and adjust for env.
# ------------------------------------------------------------------

# Flask & App Configuration

# Port for the Flask application (default: 5000)
FLASK_PORT=5001

# Base URL for the application (used in test scripts)
BASE_URL=http://127.0.0.1:5001

# Random secret for Flask sessions/CSRF (set a strong unique string in prod).
FLASK_SECRET_KEY='change-me'

# Version string to bust caches on deploys (e.g., v1.0.15).
ASSETS_VERSION=v1.0.20

# HTTP cache max-age (seconds) for static assets.
STATIC_MAX_AGE=5

# Number of server workers/threads (depends on deployment).
WORKERS=4

# Default timezone for display
DEFAULT_DISPLAY_TIMEZONE=Asia/Kolkata

# -------------------------------------------------------------------
# Database Configuration
# -------------------------------------------------------------------
# SQLAlchemy DSN (sqlite:///file.db or postgresql+psycopg2://user:pass@host:5432/db).
DATABASE_URL=sqlite:///image_manager.db


# -------------------------------------------------------------------
# Directory Configuration
# -------------------------------------------------------------------
# Upload directories for ZIP processing
UPLOAD_DIR=files/zip_upload_zips
IMAGE_DIR=files/zip_upload_images
PDF_DIR=files/zip_upload_pdfs
PROCESSED_DIR=files/zips_upload_processed
PROCESSING_ERROR_DIR=files/zip_upload_processing_error

# Directories for disease-specific PDFs
DR_PDF_DIR=files/dr_pdfs
GLAUCOMA_PDF_DIR=files/glaucoma_pdfs

# Directory for direct image uploads
DIRECT_UPLOAD_DIR=files/direct_uploads

# Logging directory
LOG_DIR=logs

# -------------------------------------------------------------------
# Upload Limits & Validation
# -------------------------------------------------------------------

# Max total request size (bytes). Keep reverse proxy limits in sync.
MAX_CONTENT_LENGTH=224288000

# Max size in bytes for a single uploaded file.
PER_FILE_MAX_BYTES=10485760

# Max number of files allowed per upload request.
MAX_FILES_PER_UPLOAD=50


# Direct Uploads Configuration

# Cap on number of images allowed per direct upload batch.
DIRECT_UPLOAD_MAX_FILES=100

# Per-file size cap (MB) for direct uploads.
DIRECT_UPLOAD_MAX_FILE_SIZE_MB=5

# Comma-separated list of allowed MIME types for direct uploads.
DIRECT_UPLOAD_ALLOWED_MIMETYPES="image/jpeg,image/png"

# -------------------------------------------------------------------
# Session & Security Configuration
# -------------------------------------------------------------------

# Session cookie settings
# SESSION_COOKIE_SAMESITE: Controls when cookies are sent with cross-site requests
#   - Lax: Allows cookies for same-site navigation and safe top-level navigation (recommended)
#   - Strict: Only allows cookies for same-site requests (more secure, may break some functionality)
#   - None: Allows cookies for all requests (requires SESSION_COOKIE_SECURE=true)
SESSION_COOKIE_SAMESITE=Lax

# SESSION_COOKIE_SECURE: Ensures cookies are only sent over HTTPS
#   - Set to 'true' in production environments for security
#   - Set to 'false' for local development with HTTP
SESSION_COOKIE_SECURE=false

# SESSION_COOKIE_HTTPONLY: Prevents JavaScript access to cookies
#   - Always keep as 'true' to prevent XSS attacks from stealing session cookies
#   - This is safe because the application does not access cookies from JavaScript
#   - The app only uses localStorage for non-sensitive UI state (activity tracking, temporary grading selections)
SESSION_COOKIE_HTTPONLY=True

# Inactivity timeout (minutes) for automatic logout
#   - Users will be logged out after this many minutes of inactivity
#   - Consider your security requirements vs. user experience when setting this value
INACTIVITY_TIMEOUT_MINUTES=30

# Security pepper for password hashing (optional but recommended)
AUTH_PEPPER=

# CSRF protection time limit (seconds)
WTF_CSRF_TIME_LIMIT=3600

# ===================================================================
# RATE LIMITING CONFIGURATION (Flask-Limiter 4.0)
# ===================================================================

# Core Rate Limiting Settings
# -------------------------------------------------------------------
# Enable/disable rate limiting globally (default: true)
RATELIMIT_ENABLED=true

# Default rate limit applied to all routes (format: "count per period")
RATELIMIT_DEFAULT=500 per hour, 50 per minute

# Application-wide limits that apply to all routes
RATELIMIT_APPLICATION=1000 per hour, 100 per minute

# Rate limiting strategy (default: fixed-window)
# Options: fixed-window, moving-window, sliding-window-counter
RATELIMIT_STRATEGY=fixed-window

# Rate limit headers configuration
RATELIMIT_HEADERS_ENABLED=true
RATELIMIT_HEADER_LIMIT=X-RateLimit-Limit
RATELIMIT_HEADER_REMAINING=X-RateLimit-Remaining
RATELIMIT_HEADER_RESET=X-RateLimit-Reset

# Rate limit behavior configuration
RATELIMIT_FAIL_ON_FIRST_BREACH=false
RATELIMIT_SWALLOW_ERRORS=true
RATELIMIT_DEDUPLICATE=false
RATELIMIT_DEFAULTS_PER_METHOD=false
RATELIMIT_DEFAULTS_COST=1
RATELIMIT_DEFAULTS_EXEMPT=

# Rate limit key configuration
RATELIMIT_KEY_PREFIX=
# RATELIMIT_KEY_FUNC=utils.rate_limiter.get_rate_limit_key

# Rate limit callback configuration
# RATELIMIT_ON_BREACH=utils.rate_limiter.handle_rate_limit_exceeded

# Role-based rate limit overrides (optional)
# ADMIN_RATE_LIMIT=5000 per day, 1000 per hour
# DATA_MANAGER_RATE_LIMIT=2000 per day, 500 per hour
# USER_RATE_LIMIT=1000 per day, 200 per hour
# AUTHENTICATED_RATE_LIMIT=1000 per day, 200 per hour
# ANONYMOUS_RATE_LIMIT=100 per day, 20 per hour

# Storage Backend Configuration
# -------------------------------------------------------------------
# Primary storage backend for rate limiting
# Options: memory:// (development), redis:// (production), memcached:// (alternative)
RATELIMIT_STORAGE_URI=memory://

# Storage options for backend (JSON format)
# RATELIMIT_STORAGE_OPTIONS={"socket_connect_timeout": 5, "socket_timeout": 5}

# Redis Configuration (Preferred for Production)
# -------------------------------------------------------------------
# Redis connection URL (takes precedence over individual settings)
# REDIS_URL=redis://localhost:6379/0

# Redis storage options (JSON format for connection pool parameters)
# RATELIMIT_STORAGE_OPTIONS={"socket_connect_timeout": 5, "socket_timeout": 5, "client_name": "flask-limiter"}

# Redis Cluster Configuration
# REDIS_STORAGE_URI=redis+cluster://localhost:7000,localhost:7001,localhost:7002

# Memcached Configuration (Alternative to Redis)
# -------------------------------------------------------------------
# Memcached server for distributed rate limiting
# RATELIMIT_STORAGE_URI=memcached://localhost:11211
# Memcached Cluster: memcached://server1:11211,server2:11211,server3:11211

# MongoDB Configuration (Alternative)
# -------------------------------------------------------------------
# MongoDB storage backend
# RATELIMIT_STORAGE_URI=mongodb://localhost:27017

# Shared Resource Limits
# -------------------------------------------------------------------
# Default shared limit for protecting specific resources
RATELIMIT_SHARED_DEFAULT=100 per hour

# Legacy Configuration (Deprecated)
# -------------------------------------------------------------------
# DISABLE_RATE_LIMITING=false  # Use RATELIMIT_ENABLED instead


# -------------------------------------------------------------------
# Email Configuration (for notifications)
# -------------------------------------------------------------------

# SMTP server settings
SMTP_SERVER=localhost
SMTP_PORT=587
SMTP_USERNAME=xxxx
SMTP_PASSWORD=yyyyy
FROM_EMAIL=

# Email SSL/TLS settings
SMTP_USE_SSL=false

# Enable debug logging for emails
EMAIL_DEBUG_LOGGING=false

# -------------------------------------------------------------------
# Logging Configuration
# -------------------------------------------------------------------

# Path to various log files
HTTP_SUCCESS_LOG=logs/http_success.log
HTTP_ERROR_LOG=logs/http_error.log
ZIP_INGEST_LOG=logs/zip_main_process_log.txt
SUCCESS_LOG=logs/process_pdf_success_log.txt
ERROR_LOG=logs/process_pdf_error_log.txt
MALICIOUS_UPLOAD_LOG=logs/malicious_uploads.log

# Log viewer configuration
LOG_VIEWER_ROOT=logs
LOG_VIEWER_MAX_BYTES=500000

# -------------------------------------------------------------------
# Pagination & UI Configuration
# -------------------------------------------------------------------


# Default page size for various listings
UPLOADED_RESULTS_PAGE_SIZE=25
SCREENINGS_PAGE_SIZE=50
ANALYTICS_SEARCH_IMAGES_PAGE_SIZE=50

# -------------------------------------------------------------------
# Grading & Arbitration Configuration
# -------------------------------------------------------------------
# Hours within  which arbitrator can revise grades
ARBITRATOR_REVISION_HOURS=6

# -------------------------------------------------------------------
# Playwright Testing Configuration
# -------------------------------------------------------------------

# Test user credentials for E2E testing
PLAYWRIGHT_USERNAME=admin
PLAYWRIGHT_PASSWORD=your_test_password

# Role-specific test credentials (optional)
PLAYWRIGHT_ADMIN_USERNAME=admin
PLAYWRIGHT_ADMIN_PASSWORD=admin_test_password
PLAYWRIGHT_USER_USERNAME=test_user
PLAYWRIGHT_USER_PASSWORD=user_test_password
PLAYWRIGHT_VIEWER_USERNAME=viewer
PLAYWRIGHT_VIEWER_PASSWORD=viewer_test_password
