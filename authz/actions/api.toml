[[actions]]
name = "api.lookups.view"
domain = "api"
zone = "api"
description = "Read API lookup data such as hospitals, lab units, diseases, grades, AI models, and scoping context."
resource_type = "lookup"
requires_resource = false

[[actions]]
name = "api.lookups.manage"
domain = "api"
zone = "api"
description = "Mutate API-managed lookup or configuration resources."
resource_type = "lookup"
requires_resource = true

[[actions]]
name = "api.viewer_settings.manage"
domain = "api"
zone = "api"
description = "Read and mutate authenticated viewer settings and presets."
resource_type = "viewer_settings"
requires_resource = false

[[actions]]
name = "api.ocr.manage"
domain = "api"
zone = "api"
description = "Read, override, or batch-process OCR/PII metadata."
resource_type = "image"
requires_resource = false

[[actions]]
name = "api.mobile.session.manage"
domain = "api"
zone = "mobile"
description = "Manage mobile authentication sessions for the authenticated account."
resource_type = "mobile_session"
requires_resource = false
